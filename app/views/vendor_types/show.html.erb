<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @vendor_type.title %>
</p>

<%= link_to 'Edit', edit_vendor_type_path(@vendor_type) %> |
<%= link_to 'Back', vendor_types_path %>
