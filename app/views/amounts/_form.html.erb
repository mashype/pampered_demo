<%= simple_form_for(@amount) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :cost %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
