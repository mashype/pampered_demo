
%li.control-group.nested-fields
	.controls
		= f.simple_fields_for :vendor_service do |vendor_service|
			= f.input :service_id, collection: VendorType.all, as: :grouped_select, group_method: :services
			= f.input :notes, :placeholder => "Notes"
		= link_to_remove_association "Remove Service", f, class: "btn btn-danger"
		%br
		%br	
 